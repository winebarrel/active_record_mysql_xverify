RSpec.describe ActiveRecordMysqlXverify do
  it 'has a version number' do
    expect(ActiveRecordMysqlXverify::VERSION).not_to be nil
  end

  it 'does something useful' do
    pending
    expect(false).to eq(true)
  end
end
